main()
{
    int x=5;

    if (x=4)  // here assignment operator is used and x contain a non-zero digit which is always true
        printf("Hello %d",x);
    else
        printf("Else statement has executed.");
    getch();
}
